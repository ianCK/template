// 0-indexed
// init(n)
// if all deg > sqrt(2m), then brute 2^n
// else find one deg(v) <= sqrt(2m), brute 2^deg(v), then cal S - v
// O(2^sqrt(2m) * n)
template <int size> Maximum_Clique {
	bitset<size> graph[size];
	void init(int n) {
		
	}
	int solve() {
		
	}
};
